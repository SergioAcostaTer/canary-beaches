# Application Name
spring.application.name=beaches

# Server Configuration
server.servlet.context-path=/api

# PostgreSQL Connection (Neon.tech)
spring.datasource.url=jdbc:postgresql://ep-solitary-violet-a9vn724d-pooler.gwc.azure.neon.tech/canarybeaches?sslmode=require
spring.datasource.username=canarybeaches_owner
spring.datasource.password=npg_uce4POsjM0Nl

# Hikari Connection Pool Configuration
spring.datasource.hikari.maximumPoolSize=10
spring.datasource.hikari.minimumIdle=5
spring.datasource.hikari.idleTimeout=300000
spring.datasource.hikari.maxLifetime=1800000
spring.datasource.hikari.connectionTimeout=30000
spring.datasource.hikari.keepaliveTime=1500000
spring.datasource.hikari.connectionTestQuery=SELECT 1

# JPA/Hibernate Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.open-in-view=false

# Flyway/Liquibase Configuration
spring.flyway.enabled=false
spring.liquibase.enabled=false

# CSV File Path
csv.file.path=beaches.csv

# Logging Configuration
logging.level.com.zaxxer.hikari=DEBUG

springdoc.swagger-ui.path=/